# =============================================================================
# Aggregator overlay for the dev cluster.
# =============================================================================
# Enable runner types by uncommenting the corresponding entries below. The
# IssueOps workflow (issueops-bootstrap-runners-v2.yml in InfraCreator) can
# also rewrite this file programmatically.
# =============================================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../shared
  - dind
  # - kubernetes
  # - gpu
